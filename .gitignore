# --- build artifacts (regenerated by pipeline/run_phase_a.py) ---
/build/
/data/parquet/

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- node (future phases / UI) ---
node_modules/
dist/
.next/

# --- misc ---
.DS_Store
*.log

# NOTE: data/raw/*.csv is intentionally COMMITTED — the dataset is CC0 / public
# domain (Ergast-compiled) and small enough to track.
